services:
  - type: web
    name: reading-place
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/app.py"
    envVars:
      - key: THUNDERFOREST_API_KEY
        sync: false
